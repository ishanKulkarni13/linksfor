// import Tree from "../models/tree";
// export const createTree = async(user,req,next)=>{
//     let tree = await Tree.create({ owner: user._id, treeName: `@${user.name}` });
//     return tree;
// }